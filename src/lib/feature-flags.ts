// Set to true once Sprachagent is production-ready.
// When false: route /sprachagent stays accessible for testing,
// but is hidden from navigation, links, sitemap, and breadcrumbs.
export const SPRACHAGENT_ENABLED = false;
